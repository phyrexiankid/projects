///scr_sound(snd,prioruty,loop,pitch,gain)

if Main.sound=1 {
    audio_sound_pitch(argument0, argument3);
    audio_play_sound(argument0, argument1, argument2);
    audio_sound_gain(argument0, argument4, 0);
}
